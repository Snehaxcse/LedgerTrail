# Local `npm run dev` can omit this — requests go to /api and Vite proxies to :8000.
# For a deployed frontend, set the FastAPI origin (no trailing slash, no /api prefix).
# VITE_API_BASE_URL=https://your-api.example.com
