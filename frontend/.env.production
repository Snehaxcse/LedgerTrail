# Baked in at `npm run build`. Override in Vercel → Settings → Environment Variables.
# Use the deployed FastAPI origin, no trailing slash and no /api prefix.
# Example: VITE_API_BASE_URL=https://your-api.example.com
#
# Unset keeps the local-dev default (/api) so `npm run build` still succeeds
# without a live backend URL.
